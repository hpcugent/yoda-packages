{
    "license": "GPLv3",
    "description": "Subset of rules and policies for iRODS based YoDa required for the yoda-portal-research module",
    "exclude": ["*/tests/*", "*/tests"],
    "user": "irods",
    "group": "irods",
    "depends": "irods-ruleset-uu == {version}",
    "directories": "/etc/irods/{name}",
    "ARGS": ".=/etc/irods/{name}"
}
